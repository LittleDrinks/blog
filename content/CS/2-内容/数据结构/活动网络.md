---
title: 活动网络
description:
tags:
  - CS/数据结构
aliases:
date: 2026-06-28T22:31:43
publish: true
---
AOV 网络：无权图，顶点表示活动，[[CS/2-内容/数据结构/拓扑排序|拓扑排序]]
比如课程的依赖关系
![[Pasted image 20260630215036.png]]

AOE 网络：带权图，边表示活动
比如工程进度
![[Pasted image 20260630215201.png]]
- 事件（点）最早可能发生时间
- 事件（点）最迟允许发生时间
- 活动（边）最早可能开始时间
- 活动（边）最迟允许开始时间
- 活动（边）的时间余量，时间余量为 0 的是关键活动
```cpp
template <class ElemType, class WeightType>Status 
CriticalPath(const AdjListDirNetwork<ElemType,WeightType>&g){
     int *indegree=new int[g.GetVexNum()];
     WeightType *ve=new int[g.GetVexNum()];
     WeightType *vl=new int[g.GetVexNum()];
     LinkQueue<int> q;
     LinkStack<int> s;
     int ee, el, u, v, count=0;
     ElemType e1, e2;
     for (v=0; v < g.GetVexNum(); v++) 
	ve[v]=0;  StatIndegree(g, indegree);
	for (v=0; v < g.GetVexNum(); v++)
	    if (indegree[v] == 0)  q.EnQueue(v);
  while (!q.IsEmpty())	{
       q.DelQueue(u); s.Push(u);	count++;
       for (v=g.FirstAdjVex(u);v!=-1;v=g.NextAdjVex(u, v)){
	if (--indegree[v] == 0)	q.EnQueue(v);
	if (ve[u] + g.GetWeight(u, v) > ve[v])
		ve[v]=ve[u] + g.GetWeight(u, v);
       }
  }
 delete []indegree;
 if (count < g.GetVexNum()) {
       delete []ve;	delete []vl;
       return FAIL;			// 网g有回路
}
s.Top(u);
	for (v=0; v < g.GetVexNum(); v++)  vl[v]=ve[u];
	while (!s.IsEmpty()) {     s.Pop(u);
	     for (v=g.FirstAdjVex(u); v != -1; v=g.NextAdjVex(u, v))	         if (vl[v] - g.GetWeight(u, v) < vl[u])
		   vl[u]=vl[v] - g.GetWeight(u, v);
	}
 	for (u=0; u < g.GetVexNum(); u++)	{ 
	   for (v=g.FirstAdjVex(u);v!=-1;v=g.NextAdjVex(u, v))	{
	       ee=ve[u]; el=vl[v] - g.GetWeight(u, v);
	       if (ee == el)  {  g.GetElem(u, e1);g.GetElem(v, e2);
		cout << "<" << e1 << ", " << e2 << "> ";
	       }
	}
     }
    delete []ve;  delete []vl;   return SUCCESS;
}

```