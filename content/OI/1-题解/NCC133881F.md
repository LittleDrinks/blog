---
title: NCC133881F
description:
tags:
  - OI/题解
  - OI/exKMP
  - OI/DP/状压DP
aliases:
date: 2026-08-06T14:57:53
publish: true
---
## [NCC133881F](https://ac.nowcoder.com/acm/contest/133881/F)

0-base
先求出 Z 函数，特判 $z_i=n-i$ 时直接输出 0。对于每个后缀 $i$，相当于要求
$$
s[z_i]<s[i+z_i]
$$
这个问题等价于求 DAG 的拓扑序数量，直接状压 DP 即可，时间复杂度 $O(n+\alpha2^{\alpha})$

[***AC 代码***](https://ac.nowcoder.com/acm/contest/view-submission?submissionId=84386885)

```cpp
#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using db = long double;
using i128 = __int128;

template<typename T1,typename T2> ostream& operator<<(ostream& os, const pair<T1,T2>& p) {
    return os<<"("<<p.first<<","<<p.second<<")";
}
template<typename T> ostream& operator<<(ostream& os, const vector<T>& v) {
    os<<"["; for(int i=0;i<v.size();++i) os<<(i?", ":"")<<v[i]; return os<<"]";
}
template<typename T> void gmax(T&x,T y){ x = max(x, y); }
template<typename T> void gmin(T&x,T y){ x = min(x, y); }

vector<int> Z_function(string s) {
    int n = s.size();
    vector<int> z(n);
    for (int i=1, l=0, r=0; i < n; ++i) {
        if (i <= r && z[i-l] < r-i+1) {
            z[i] = z[i-l];
        } else {
            z[i] = max(0, r-i+1);
            while (i+z[i] < n && s[z[i]] == s[i+z[i]]) ++z[i];
        }
        if (i+z[i]-1 > r) {
            l = i;
            r = i + z[i] - 1;
        }
    }
    return z;
}

int main() {
    ios::sync_with_stdio(0); cin.tie(0); cout.tie(0); 
    string s;
    cin >> s;
    int n = s.size();
    vector<int> z = Z_function(s);

    vector<int> msk(26);
    for (int i = 1; i < n; ++i) {
        if (z[i] == n-i) {
            cout << "0\n";
            return 0;
        }
        int a = s[z[i]]-'a';
        int b = s[i+z[i]]-'a';
        if (a != b) {
            msk[a] |= (1 << b);
        }
    }

    vector<unsigned> dp(1<<26);
    dp[0] = 1;
    for (int S = 0; S < (1<<26); ++S) {
        for (int i = 0; i < 26; ++i) {
            if (S >> i & 1) continue;
            if ((S & msk[i]) == msk[i]) {
                dp[S^(1<<i)] += dp[S];
            }
        }
    }
    cout << dp.back() << "\n";
}
```
